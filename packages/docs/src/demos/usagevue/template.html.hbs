---
category: Usage Vue Component
title: Template
layout: demo.html
docVersion: 0.5.0
order: 100
---

{{#marked}}
## Template


{{#if lang_ja}}
### 利用可能な`Vue Component`のタグ名

| Component Tag Name | 機能 | 補足 | LINK |
|----------|-----|---|---|
| `<c-grid>` | グリッド本体 | --- | --- |
| `<c-grid-column>` | グリッドに表示するカラム定義 | --- | --- |
| `<c-grid-column-group>` | ヘッダを階層表示にする際のカラムグループの定義 | --- | --- |
| `<c-grid-button-column>` | ボタンカラム定義 | ボタン用カラム定義のシンタックスシュガー | --- |
| `<c-grid-input-column>` | 入力カラム定義 | 入力用カラム定義のシンタックスシュガー | --- |
| `<c-grid-menu-column>` | メニューカラム定義 | メニュー用カラム定義のシンタックスシュガー | --- |
| `<c-grid-link-column>` | リンクカラム定義 | リンク用カラム定義のシンタックスシュガー | --- |
| `<c-grid-icon-column>` | アイコンカラム定義 | アイコン用カラム定義のシンタックスシュガー | --- |
| `<c-grid-percent-complete-bar-column>` | パーセントバーカラム定義 | パーセントバー用カラム定義のシンタックスシュガー | --- |
{{else}}
### Available `Vue Component` tag names

| Component Tag Name | Function | Others | LINK |
|----------|-----|---|---|
| `<c-grid>` | Grid body | --- | --- |
| `<c-grid-column>` | Column definition to display in the grid | --- | --- |
| `<c-grid-column-group>` | Definition of column group when multiple header is displayed | --- | --- |
| `<c-grid-button-column>` | Button column definition | Syntax sugar for button column definition | --- |
| `<c-grid-input-column>` | Input column definition | Syntax sugar for input column definition | --- |
| `<c-grid-menu-column>` | Menu column definition | Syntax sugar for menu column definition | --- |
| `<c-grid-link-column>` | Link column definition | Syntax sugar for linkable column definition | --- |
| `<c-grid-icon-column>` | Icon column definition | Syntax sugar for icon column definition | --- |
| `<c-grid-percent-complete-bar-column>` | Percent complete bar column definition | Syntax sugar for Percent complete bar column definition | --- |
{{/if}}

{{/marked}}


<script type="text/javascript">
'use strict';
//{{{babel textContents.demos.[demos/init.parts.js]}}}
const records = generatePersons(10000);
</script>

{{#vue}}
{{#copy "vue1"}}
<template>
  <div class="demo-grid middle">
    <c-grid
      :data="records"
      :frozen-col-count="1">
      <c-grid-column
        :width="50"
        field="check"
        column-type="check"
        action="check" />
      <c-grid-column
        field="personid"
        width= "85"
      >
        ID
      </c-grid-column>
      <!-- multiple header -->
      <c-grid-column-group
        caption="Name">
        <c-grid-input-column
          field= "fname"
          width= "20%"
          min-width="150"
        >
          First Name
        </c-grid-input-column>
        <c-grid-input-column
          field="lname"
          width= "20%"
          min-width="150"
        >
          Last Name
        </c-grid-input-column>
      </c-grid-column-group>
      <!-- button -->
      <c-grid-button-column
        caption="SHOW REC"
        width="120"
        @click="onClickRecord"
      />
    </c-grid>
  </div>
</template>

<script>
export default {
	name: 'App',
	data: {
		records
	},
	methods: {
		onClickRecord(rec) { alert(JSON.stringify(rec)) }
	}
}
</script>
{{/copy}}
{{/vue}}

{{> code class="vue" code=vue1}}
