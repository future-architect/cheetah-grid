---
"cheetah-grid-playwright": minor
---

Introduce `cheetah-grid-playwright`: Playwright helpers for driving Cheetah Grid in browser automation. Locates cells through the grid API (`getInstanceByElement`, `getCellRangeByField`, `getCellRelativeRect`) and operates on them with real mouse and keyboard events.
